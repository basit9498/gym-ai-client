import { RegisterForm } from '@/modules/auth';

export const metadata = {
  title: 'Sign Up | BodyForgeAI Registration',
  description: 'Join the world\'s first BodyForgeAI ecosystem.',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
